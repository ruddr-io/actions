name: Create GITHUB_REF slug
description: Convert the GITHUB_REF to a simple slug that can be used in URIs, filenames, and IDs.
outputs:
  slug:
    description: The string that was generated from the GITHUB_REF.
  _slug:
    description: The slug with underscores instead of hyphens.
runs:
  using: node12
  main: index.js
